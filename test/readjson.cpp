#include <iostream>
#include <stdlib.h>
#include "../jsonXX.h"
using namespace std;
using namespace json;

int main(int argc, char* argv[]) {
    /***
    if(argc < 0) {
        
    }
    char* infilename = argv[1];
    ifstream fin(infilename);
    ***/
    var json;
    cin >> json;
    cout << json << endl;
}
